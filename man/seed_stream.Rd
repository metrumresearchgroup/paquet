% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stream-seed.R
\name{seed_stream}
\alias{seed_stream}
\title{Assign reproducible RNG seeds to a file stream}
\usage{
seed_stream(x, seed)
}
\arguments{
\item{x}{a \code{file_stream} object.}

\item{seed}{a single integer passed to \code{\link[=set.seed]{set.seed()}}.}
}
\value{
\code{x} is returned with a \code{seed} element added to every item.
}
\description{
Seeds each item in a \code{file_stream} list with an independent L'Ecuyer-CMRG
RNG stream so that parallel workers produce fully reproducible results. Call
\code{\link[=set_stream_seed]{set_stream_seed()}} inside the worker function to activate the seed before
running any stochastic code.
}
\details{
Internally calls \code{set.seed(seed, kind = "L'Ecuyer-CMRG")} and then advances
the RNG state across items using \code{\link[parallel:RngStream]{parallel::nextRNGStream()}}. The caller's
\code{.Random.seed} (both kind and state) is restored on, so the parent process
RNG is unaffected.
}
\examples{
s <- new_stream(4)
s <- seed_stream(s, seed = 123)
length(s[[1]]$seed)

}
\seealso{
\code{\link[=set_stream_seed]{set_stream_seed()}}, \code{\link[=new_stream]{new_stream()}}
}
