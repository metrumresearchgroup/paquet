# List all output files in a fst file set

Use the function to read all of the `.fst` files that were saved when
`bg_mrgsim_d` was called and `.path` was passed along with
`.format = "fst"`.

## Usage

``` r
list_fst(path)
```

## Arguments

- path:

  The (full) directory path to search.
